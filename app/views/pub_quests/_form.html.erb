<%= form_for(@pub_quest) do |f| %>
  <% if @pub_quest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pub_quest.errors.count, "error") %> prohibited this pub_quest from being saved:</h2>

      <ul>
      <% @pub_quest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  

  <%= f.fields_for :choices do |builder| %>
  <%= render "choice_fields", :f => builder %>
  <% end %>
   <p><%= link_to_add_fields "Add Option", f, :choices %></p>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
